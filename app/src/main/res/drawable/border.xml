<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <corners android:radius="20dp" /> <!-- Устанавливаем радиус для закругления -->
    <solid android:color="@android:color/transparent" /> <!-- Цвет фона, если нужен -->
    <stroke android:width="1dp" android:color="@android:color/black" /> <!-- Рамка, если нужна -->
</shape>